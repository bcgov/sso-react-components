import styled from 'styled-components';

const Header = styled.header`
  &.text-bold {
    font-weight: bold;
  }
  &.text-22px-font {
    font-size: 22px;
    font-weight: bold;
  }
  &.text-bold-blue {
    font-size: 22px;
    font-weight: bold;
    color: #0139ff;
  }
`;

export default Header;
